var conf = require('../../nightwatch.conf.js');

module.exports = {
  'Open Website Vente privée': function (browser) {
    browser

  },

  'Check block login': function (browser) {
    browser

  },

  'Email field must exist': function (browser) {
    browser

  },

  'Password field must exist': function (browser) {
    browser

  },

  'Button Ok validation must exist': function (browser) {
    browser

    browser.end();
  }
}
